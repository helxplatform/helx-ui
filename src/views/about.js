import { Fragment } from 'react'
import { Typography } from 'antd'

const { Title } = Typography

export const AboutView = () => {
  return (
    <Fragment>
      <Title level={ 1 }>About HeLx</Title>
      ...
    </Fragment>
  )
}