import { Divider } from 'antd'

export const AccountPasswordTab = ({ }) => {
    return (
        <div style={{ display: "flex", flexDirection: "column", height: "100%" }}>
            <Divider orientation="left" orientationMargin={ 0 } style={{ marginBottom: 24 }}>Password information</Divider>
        </div>
    )
}